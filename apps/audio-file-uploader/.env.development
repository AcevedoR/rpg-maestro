PORT=3001

# validate user access tokens against the same issuer as rpg-maestro (fake IDP served by rpg-maestro in dev)
AUTH_ISSUER=http://localhost:3000/test-utils/fake-idp
# shared secret for server-to-server calls from rpg-maestro (fake value, dev only)
AUDIO_FILE_UPLOADER_SERVICE_TOKEN=dev-service-token
