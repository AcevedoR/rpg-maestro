DATABASE=in-memory
DEFAULT_AUDIO_FILE_UPLOADER_API_URL=http://localhost:3001/api
DEFAULT_FRONTEND_DOMAIN=http://localhost:4200
PORT=3000

AUTH_JWT_AUDIENCE=http://localhost:3000
AUTH_ISSUER=http://localhost:3000/test-utils/fake-idp

NODE_ENV=dev
CONFIGURATION_ENV=dev

# shared secret sent to audio-file-uploader on server-to-server calls (fake value, dev only)
AUDIO_FILE_UPLOADER_SERVICE_TOKEN=dev-service-token
