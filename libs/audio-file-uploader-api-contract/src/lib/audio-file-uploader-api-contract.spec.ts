import { audioFileUploaderApiContract } from './audio-file-uploader-api-contract';

describe('audioFileUploaderApiContract', () => {
  it('should work', () => {
    expect(audioFileUploaderApiContract()).toEqual('audio-file-uploader-api-contract');
  });
});
